{
  "entities": {
    "SearchReport": {
      "title": "Search Report",
      "description": "A generated PDF report of a search result",
      "type": "object",
      "properties": {
        "uid": { "type": "string", "description": "The user ID who generated the report" },
        "query": { "type": "string", "description": "The search query (username or email)" },
        "searchType": { "type": "string", "enum": ["username", "email"] },
        "pdfUrl": { "type": "string", "format": "uri", "description": "The URL to the stored PDF in Cloud Storage" },
        "createdAt": { "type": "string", "format": "date-time", "description": "When the report was generated" }
      },
      "required": ["uid", "query", "searchType", "pdfUrl", "createdAt"]
    },
    "SearchHistory": {
      "title": "Search History",
      "description": "A record of a search performed by a user",
      "type": "object",
      "properties": {
        "uid": { "type": "string", "description": "The user ID who performed the search" },
        "query": { "type": "string", "description": "The search query (username or email)" },
        "searchType": { "type": "string", "enum": ["username", "email"] },
        "createdAt": { "type": "string", "format": "date-time", "description": "When the search was performed" }
      },
      "required": ["uid", "query", "searchType", "createdAt"]
    }
  },
  "firestore": {
    "/reports": {
      "schema": "SearchReport",
      "description": "Collection of search reports generated by users"
    },
    "/searches": {
      "schema": "SearchHistory",
      "description": "Collection of searches performed by users"
    }
  }
}
